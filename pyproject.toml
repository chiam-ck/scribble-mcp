[project]
name = "scribble-mcp"
version = "1.1.0"
description = "Direct HTTP API for iCloud-backed Obsidian vaults on Linux and macOS"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
vault-api = "vault_api:main"
